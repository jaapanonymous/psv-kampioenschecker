[project]
name = "psv-kampioenschecker"
version = "0.1.0"
description = "Check wanneer PSV kampioen wordt"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "requests",
    "python-dotenv",
    "jinja2",
]

[tool.uv]
managed = true
package = false

